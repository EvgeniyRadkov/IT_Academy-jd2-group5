version: "3.9"
services:
  postgres:
    image: postgres:13.3
    environment:
      POSTGRES_DB: "service"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "pgpwd4itacademy"
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - "5432:5432"